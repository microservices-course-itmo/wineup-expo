import { render } from '@testing-library/react-native'
import React from 'react'
import RecommendedFilterPage from './index'

it('should rightly rendered <RecommendedFilterPage/>', () => {
  render(<RecommendedFilterPage />)
})
