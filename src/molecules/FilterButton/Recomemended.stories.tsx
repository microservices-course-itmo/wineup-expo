import React from 'react'
import { storiesOf } from '@storybook/react-native'
import RecommendedFilterButton from './Price'

storiesOf('RecommendedFilterButton', module).add('Default', () => (
  <RecommendedFilterButton />
))
