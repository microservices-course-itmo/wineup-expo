import React from 'react'
import { storiesOf } from '@storybook/react-native'
import CountryFilterButton from './Country'

storiesOf('CountryFilterButton', module).add('Default', () => (
  <CountryFilterButton />
))
