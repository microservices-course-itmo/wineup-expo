import React from 'react'
import { storiesOf } from '@storybook/react-native'
import ColorFilterButton from './Color'

storiesOf('ColorFilterButton', module).add('Default', () => (
  <ColorFilterButton />
))
