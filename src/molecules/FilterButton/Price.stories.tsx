import React from 'react'
import { storiesOf } from '@storybook/react-native'
import PriceFilterButton from './Price'

storiesOf('PriceFilterButton', module).add('Default', () => (
  <PriceFilterButton />
))
