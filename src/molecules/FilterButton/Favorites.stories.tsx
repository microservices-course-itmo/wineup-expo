import React from 'react'
import { storiesOf } from '@storybook/react-native'
import FavoritesFilterButton from './Favorites'

storiesOf('FavoritesFilterButton', module).add('Default', () => (
  <FavoritesFilterButton />
))
