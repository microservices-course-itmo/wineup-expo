import React from 'react'
import { storiesOf } from '@storybook/react-native'
import GoBackArrowIcon from './GoBackArrowIcon'

storiesOf('ConsentCheckBox', module).add('Default', () => <GoBackArrowIcon />)
