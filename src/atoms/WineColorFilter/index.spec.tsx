import { render } from '@testing-library/react-native'
import React from 'react'
import WineColorFilter from './index'

describe('<WineColorFilter />', () => {
  it('should render', () => {
    render(<WineColorFilter />)
  })
})
